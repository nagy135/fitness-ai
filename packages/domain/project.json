{
  "name": "domain",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "packages/domain/src",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "vitest run packages/domain" }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc -p packages/domain/tsconfig.json --noEmit" }
    }
  },
  "tags": ["scope:shared", "type:domain"]
}
